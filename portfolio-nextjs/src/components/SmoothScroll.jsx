'use client';

// SmoothScroll wrapper - now using native browser scrolling
// Lenis has been removed due to scrolling issues

const SmoothScroll = ({ children }) => {
    return <>{children}</>;
};

export default SmoothScroll;

